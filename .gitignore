# Compiled binary (root only, not cmd/crobot/)
/crobot

# Sensitive config and output files
.crobot.yaml
findings.json
*.env
# Compiled mock agent binary (but not the source in testdata/)
/mockagent

# Claude Code local settings (may contain permissions/paths)
.claude/settings.local.json
